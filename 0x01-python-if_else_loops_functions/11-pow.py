#!/usr/bin/python3
def pow(a, b):
    x = 1
    if b >= 0:
        for i in range(0, b):
            x = x * a
        return (x)
    else:
        b2 = -1 * b
        for i in range(0, b2)
            x = x * a
    return (1 / x)
